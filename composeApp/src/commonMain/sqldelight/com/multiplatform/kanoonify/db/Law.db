CREATE TABLE Law (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    category TEXT NOT NULL
);

selectAll:
SELECT * FROM Law;

insertLaw:
INSERT INTO Law(title, description, category)
VALUES (?, ?, ?);

searchLaw:
SELECT * FROM Law
WHERE title LIKE ? OR description LIKE ?;